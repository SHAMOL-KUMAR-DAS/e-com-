import 'package:flutter/material.dart';

const colors = Color(0xFF019877);

const backColor = Color(0xFFe4e9ec);

const textFormColor = Color(0xFFffffff);

const buttonText = TextStyle(fontSize: 18, color: Colors.white, fontWeight: FontWeight.bold);